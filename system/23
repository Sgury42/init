	# Expliquez comment se connecter en tant que ce nouvel utilisateur. (En session
	# graphique et en session ssh)

session graphique: au demarage de la machine, login / password

session ssh: ssh user_name@IP / password (commande su pour changer d'utilisateur)
